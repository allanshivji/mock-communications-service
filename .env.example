# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration
# For Docker: use 'postgres', for local development: use 'localhost'
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=mock_communications

# Redis Configuration
# For Docker: use 'redis', for local development: use 'localhost'
REDIS_HOST=localhost
REDIS_PORT=6379


# AWS S3 Configuration (Mock - not actually used, no real AWS)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=mock_access_key
AWS_SECRET_ACCESS_KEY=mock_secret_key
S3_BUCKET_NAME=mock-recordings-bucket

# Rate Limiting Configuration
MAX_CONCURRENT_CALLS_PER_KEY=3
MAX_CPS_PER_KEY=2

# API Keys (comma-separated list)
VALID_API_KEYS=test-api-key-1,test-api-key-2,demo-key